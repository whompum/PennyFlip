<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Recycler Views -->
    <item name="id_global_list" type="id"/>

    <!-- View Pagers -->
    <item name="id_global_pager" type="id"/>

    <!-- Floating Action Buttons -->
    <item name="id_global_fab" type="id"/>

    <!-- CurrencyEditTexts (big) -->
    <item name="id_global_total_display" type="id"/>

    <!-- Up button navigation -->
    <item name="id_global_nav" type="id"/>

    <!-- Timestamps -->
    <item name="id_global_timestamp" type="id"/>

    <!-- Toolbars -->
    <item name="id_global_toolbar" type="id"/>

    <!-- Generic Title id -->
    <item name="id_global_title" type="id"/>

    <!-- Strips Pager Titles-->
    <item name="id_global_strips_indicator" type="id"/>

    <!-- Search action buttons -->
    <item name="id_global_search" type="id"/>

    <!-- Text Editors -->
    <item name="id_global_editor" type="id"/>

    <!-- Sort action -->
    <item name="id_global_sort" type="id"/>


    <!-- The white search bar of the SourceList activity -->
    <item name="id_source_list_search_toolbar" type="id"/>

    <!-- Navigation icons in Dashboard -->
    <item name="id_nav_source" type="id"/>
    <item name="id_nav_statistics" type="id"/>
    <item name="id_nav_menu_history" type="id"/>

</resources>





